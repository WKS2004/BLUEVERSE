# Versioning

BLUEVERSE uses `x.y.z`.

- `x` — major project phase
- `y` — integration/development release
- `z` — bug-fix release

Current foundation line:

```text
0.y.z
```

Git tags should be used for released versions.
