# Docker Rules

- Use the selected Docker Hardened Images.
- Keep custom Dockerfiles under infrastructure/docker.
- Do not assume minimal runtime images provide a shell.
- Keep services on the intended Docker networks.
