# Security Rules

- Treat all client and model input as untrusted.
- Never commit secrets.
- Use permission-based authorization.
- Validate AI/tool outputs deterministically before business execution.
- Do not persist hidden reasoning.
